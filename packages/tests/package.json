{
  "name": "ipfs-http-tests",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "reset": "rm -rf package-lock.json && rm -rf yarn.lock && rm -rf node_modules"
  },
  "author": "",
  "license": "ISC"
}
