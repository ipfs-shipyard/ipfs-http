{
  "name": "ipfs-graphql-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "reset": "rm -rf package-lock.json && rm -rf yarn.lock && rm -rf node_modules"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "apollo-server-hapi": "^2.0.5",
    "@hapi/boom": "^7.4.2",
    "graphql": "^14.0.0",
    "ipfs-rest-client": "^1.0.0"
  },
  "devDependencies": {
    "aegir": "^15.1.0"
  }
}
